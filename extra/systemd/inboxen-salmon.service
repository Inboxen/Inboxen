[Unit]
Description=Mail router for Inboxen
After=network.target

[Service]
Type=forking
User=inboxen
Group=inboxen
EnvironmentFile=/etc/inboxen/inboxen.conf
WorkingDirectory=/srv/inboxen/
ExecStart="${SALMON_BIN} start"
ExecStop="${SALMON_BIN} stop"

[Install]
WantedBy=multi-user.target
